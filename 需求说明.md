---
title: 菠萝乐园初始需求说明
version: V1.6
status: 初始需求
date: 2026-09-03
platforms:
  - Android
  - 手机 Web
---

# 菠萝乐园初始需求说明

## 1. 文档说明

- 产品名称：菠萝乐园。
- 文档版本：V1.6。
- 文档状态：初始需求。
- 固化日期：2026-09-01。
- 目标平台：Android、手机 Web；手机 Web 用于覆盖 iPhone Safari 访问。
- 本文档是项目首个正式需求基线。V0.2 将内容来源调整为文件夹自动发现，V0.3 将 Docker 与宿主机媒体目录映射确认为正式部署方式，V0.4 增加缺失人工封面时的自动截帧与独立缓存，V0.5 增加按媒体文件夹分类展示与分类内顺序播放，V0.6 将早教页分类改为横向书架并增加骨架屏与“查看更多”完整列表页，V0.7 统一视频卡片的两行标题槽位，V0.8 将分类预览调整为“前 4 个视频 + 第 5 位查看更多”，V0.9 优化 600px 以下早教页头部与分类头的紧凑响应式布局，V1.0 完成“菠萝早教”品牌升级，V1.1 将 Flutter Web 与 Rust Service 拆分为独立容器并固定外部端口，V1.2 新增“菠萝乐园”总首页与“菠萝早教、菠萝视频、菠萝相册”三个一级分类，并将原视频书架调整为菠萝早教子页面，V1.3 将产品、Android 应用、Web/PWA 和 Flutter 应用显示名统一调整为“菠萝乐园”，V1.4 增加 Android 自托管 APK 静默升级管线，V1.5 将 APK 与 Web 的默认正式服务入口统一为 `https://wx.jiayuntong.com:5172/server/`，V1.6 精简总首页主提示和三个子页面标题栏，并修复 Web Release 一级分类图标缺失；后续范围、接口或验收标准发生变化时，应先更新本文档并记录版本变更。

## 2. 产品定位与范围

BoBoLearning 是面向儿童的轻量内容乐园，总首页以“菠萝乐园”承载菠萝早教、菠萝视频和菠萝相册三个一级分类；首个可用业务模块“菠萝早教”通过简单、直观、童趣的封面浏览和沉浸式播放体验，降低儿童选片和操作成本。

### 2.1 MVP 范围

- Rust 后端递归扫描本地媒体目录，自动发现视频；人工同名封面可选，缺失时自动截取视频画面，不要求人工维护内容清单。
- 后端提供视频列表、封面和支持拖动定位的视频流接口。
- 菠萝早教子页面按媒体文件夹分区，以横向滚动书架展示视频，并提供完整文件夹列表页。
- Flutter 启动后先进入“菠萝乐园”总首页，展示菠萝早教、菠萝视频、菠萝相册三个大入口；原视频书架作为菠萝早教子页面，并提供明确返回总首页的入口。
- 菠萝视频和菠萝相册首版提供可进入、可返回且有明确中文状态的预留子页面，不在未定义接口和内容模型的情况下复用早教视频数据。
- 点击封面进入独立沉浸式播放页。
- 播放页支持进度显示、点击或拖动进度条定位、点击画面播放或暂停。
- 上滑或下滑在当前文件夹分类内按自然排序切换视频，切换后自动播放，不跨文件夹串播。
- Flutter Web 由独立 Client 容器托管，并将同源 `/api` 请求反向代理到 Rust Service 容器。
- Client 与 Service 分别构建、分别运行，媒体目录仅映射到 Service 容器。
- 产品界面、状态文案、错误提示和运行提示使用中文；平台应用、Android 安装名称、浏览器页签及 Web/PWA 显示名统一使用“菠萝乐园”，“菠萝早教”保留为一级业务分类及其子页面名称。
- Android 启动及回到前台时静默检查新版本；发现更高版本后由系统后台下载，校验通过才弹出“重启并安装”，用户点击后进入 Android 系统安装确认流程。

### 2.2 MVP 不包含

- 数据库、用户登录、权限体系和播放历史。
- 视频上传、编辑、删除、排序后台或管理端页面。
- 搜索、收藏、推荐、评论和付费功能，以及独立维护分类的管理后台。
- 原生 iOS 客户端。
- CDN、对象存储、视频转码和自适应码率流媒体。
- 正式视频和封面素材。
- 菠萝视频和菠萝相册的正式内容模型、资源接口、管理能力与浏览功能。

## 3. 总体技术架构

### 3.1 项目组成

- `Client`：Flutter 客户端，支持 Android 和 Web；Web Release 由独立 Nginx 容器托管并反向代理 `/api`。
- `Service`：Rust 服务，负责媒体目录自动发现、视频与封面响应及健康检查，不再托管前端静态文件。
- `Service/media`：本地媒体根目录，可按任意业务文件夹组织多个视频与同名封面；正式媒体文件默认不纳入 Git。
- `Service/cover-cache`：本地开发的自动封面缓存目录；Docker 部署时由独立命名卷替代，不写回只读媒体目录。
- `Service/updates`：Android 升级包发布目录，保存原子发布的 `latest.json` 和版本化 APK；正式部署时以只读 bind mount 提供给 Service。

### 3.2 Rust 服务技术选型

- Rust 1.98，使用 2024 Edition。
- `axum 0.8`：HTTP 路由与请求处理。
- `tokio 1`：异步运行时。
- `serde`、`serde_json`：接口序列化。
- `tower-http 0.7`：静态文件、视频 Range、CORS 和 HTTP 中间件。
- `walkdir`：安全递归遍历媒体目录。
- `notify`：监听媒体目录新增、修改、重命名和删除事件。
- `sha2`：根据规范化相对路径生成稳定、不可直接构造文件路径的视频 ID。
- `tracing`、`tracing-subscriber`：结构化日志。
- `FFmpeg`：仅作为运行时截帧工具，从缺少人工封面的视频中提取单帧并编码为 WebP；不承担视频转码。

配置项：

| 配置项 | 默认值 | 用途 |
|---|---|---|
| `BOBO_BIND` | `0.0.0.0:8080` | HTTP 监听地址 |
| `BOBO_MEDIA_DIR` | 本地开发为 `Service/media`；容器内为 `/app/media` | 本地媒体根目录 |
| `BOBO_COVER_CACHE_DIR` | 本地开发为 `Service/cover-cache`；容器内为 `/app/cover-cache` | 自动封面持久化缓存目录 |
| `BOBO_DEFAULT_COVER_PATH` | 本地开发使用应用图标；容器内为 `/app/default-cover.png` | 自动截帧失败时使用的默认封面 |
| `BOBO_FFMPEG_BIN` | `ffmpeg` | FFmpeg 可执行文件名或绝对路径 |
| `BOBO_COVER_CAPTURE_SECS` | `3` | 自动封面优先截取秒数；失败时回退到首帧 |
| `BOBO_SCAN_DEBOUNCE_MS` | `1500` | 媒体目录变化后的防抖扫描时间 |
| `BOBO_SCAN_INTERVAL_SECS` | `10` | 周期完整扫描间隔，用于兜底宿主机映射目录未产生可靠文件事件的情况；设为 `0` 可关闭 |
| `BOBO_UPDATE_DIR` | 本地开发为 `Service/updates`；容器内为 `/app/updates` | Android 升级清单与 APK 的只读发布目录 |

### 3.3 Flutter 客户端技术选型

- Flutter 3.47.2、Dart 3.13.2。
- `http 1.6`：Android 与 Web 共用的 API 客户端。
- `video_player 2.14`：Android 与 Web 视频播放。
- Android 原生 `DownloadManager`：持久化后台下载 APK，并在网络变化或系统重启后继续处理；不在 Dart 层维持长时下载任务。
- Flutter `MethodChannel`：查询 Android 版本、下载状态并唤起系统安装器；Web 端保持禁用。
- 使用 MVVM + Repository 分层，不引入额外状态管理、路由或代码生成框架。
- 简单依赖由应用入口手工装配，保持 MVP 依赖轻量。

建议目录职责：

```text
Client/lib/
├── data/              # API 模型、服务和仓储实现
├── domain/            # 不依赖 UI 的视频领域模型
└── ui/
    ├── core/          # 主题、公共组件和设计令牌
    └── features/
        ├── home/      # 菠萝早教视频书架与 ViewModel
        ├── player/    # 播放页、播放器适配器与 ViewModel
        ├── portal/    # 菠萝乐园总首页与一级分类导航
        └── update/    # Android 静默升级状态、提示层与 ViewModel
```

### 3.4 Docker 部署架构

- 正式部署使用 Docker Compose V2；非容器方式只作为本地开发和自动化测试入口。
- 根目录提供 `compose.yaml`；`Client/Dockerfile` 构建 Flutter Web Release 并复制到精简 Nginx 运行镜像，`Service/Dockerfile` 独立构建 Rust Release 并复制到含 FFmpeg 的精简运行镜像。
- Docker 构建上下文位于项目根目录，`.dockerignore` 排除 `.git`、本地构建缓存、测试输出和整个 `Service/media` 内容，确保视频与封面不会进入镜像层。
- 两个最终镜像均不包含 Rust、Flutter、Dart、Android SDK 或其他构建工具；Client 仅保留 Nginx 与 Web 产物，Service 仅保留二进制、默认封面、FFmpeg、CA 证书和健康检查工具。
- Client 容器内部监听 `8080` 并发布到宿主机 `5170`；Service 容器内部监听 `8080` 并发布到宿主机 `5171`。
- 正式环境的 APK 与 Web 统一使用 `https://wx.jiayuntong.com:5172/server/` 作为服务根地址，客户端自动补齐 `api/v1/`；外层 Nginx 去掉 `/server/` 前缀后转发到 Service。本地开发仍可通过 `API_BASE_URL` 覆盖为 `http://<主机>:5171`。
- 两个容器均以专用非 root 用户运行、使用只读根文件系统和必要的临时 `tmpfs`，并启用 `no-new-privileges`；只有 Service 挂载只读媒体目录、只读升级目录和可写自动封面命名卷。
- Service 正确处理 `SIGTERM`，在 Compose 配置的 15 秒停止宽限期内停止监听、结束目录监听任务并退出；Client 使用 10 秒停止宽限期。

Compose 的媒体映射约定：

```yaml
services:
  service:
    build:
      context: .
      dockerfile: Service/Dockerfile
    ports:
      - "${BOBO_API_PORT:-5171}:8080"
    volumes:
      - type: bind
        source: ${BOBO_MEDIA_HOST_PATH:-./Service/media}
        target: /app/media
        read_only: true
        bind:
          create_host_path: false
      - type: volume
        source: cover-cache
        target: /app/cover-cache
    environment:
      BOBO_BIND: 0.0.0.0:8080
      BOBO_MEDIA_DIR: /app/media
      BOBO_COVER_CACHE_DIR: /app/cover-cache
      BOBO_DEFAULT_COVER_PATH: /app/default-cover.png
      BOBO_FFMPEG_BIN: ffmpeg
      BOBO_COVER_CAPTURE_SECS: ${BOBO_COVER_CAPTURE_SECS:-3}
      BOBO_SCAN_DEBOUNCE_MS: 1500
      BOBO_SCAN_INTERVAL_SECS: 10
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 15s

  client:
    build:
      context: .
      dockerfile: Client/Dockerfile
    depends_on:
      service:
        condition: service_healthy
    ports:
      - "${BOBO_WEB_PORT:-5170}:8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  cover-cache:
```

- `Service/media` 必须作为可见的宿主机资源目录随项目创建，但目录内容默认由 Git 忽略。
- 媒体使用 bind mount 而非 Docker 命名卷，便于直接通过资源管理器向宿主机目录放入、替换或删除视频和人工封面。
- 映射在容器内为只读，后端不得创建、修改或删除宿主机资源；资源维护完全由宿主机负责。
- 自动封面使用独立可写 Docker 命名卷，容器重建后继续复用；删除该卷只会触发重新截帧，不影响媒体文件。
- `BOBO_MEDIA_HOST_PATH` 允许改为任意已存在的宿主机绝对路径；Compose 不自动创建不存在的来源目录，路径错误时必须明确启动失败。
- `BOBO_WEB_PORT` 和 `BOBO_API_PORT` 分别调整前端与后端宿主机发布端口，默认值固定为 `5170` 与 `5171`，两个容器内部端口均保持 `8080`。
- Compose 为两个容器分别配置健康检查；Client 必须等待 Service 健康后启动，两个容器均健康才视为部署成功。
- Client Nginx 保持 Web 与 `/api` 同源；Service 的独立发布端口用于 Android 与接口调试。

## 4. 媒体目录与自动发现

### 4.1 目录组织

媒体根目录下允许创建任意层级的业务文件夹；一个文件夹内可以放置多个视频。人工封面可选，提供时必须与视频位于同一文件夹且基础文件名完全相同：

```text
Service/media/
├── 动物世界/
│   ├── 小猫.mp4
│   ├── 小猫.webp
│   └── 小狗.mp4
└── 颜色启蒙/
    ├── 01-认识红色.mp4
    ├── 01-认识红色.png
    └── 02-认识蓝色.mp4
```

配对与解析规则：

- 首版只发现扩展名为 `.mp4` 的视频，扩展名匹配不区分大小写。
- 人工封面支持 `.webp`、`.png`、`.jpg`、`.jpeg`，扩展名匹配不区分大小写。
- 人工封面与视频必须位于同一文件夹，且除扩展名外的文件名完全相同；人工封面始终优先于自动封面。
- 视频标题直接取视频文件名去除扩展名后的文本，保留中文、空格、数字和原有标点。
- 视频分类直接取视频相对于媒体根目录的父文件夹路径，路径分隔符统一为 `/`；例如 `启蒙/英语/单词.mp4` 的分类路径为 `启蒙/英语`。
- 媒体根目录中的视频分类路径为空字符串，客户端统一展示为“未分类”；空文件夹不产生分类。
- 同一视频存在多个同名封面时，按 WebP、PNG、JPG、JPEG 的优先级选取，并输出中文警告。
- 缺少人工封面时，服务调用 FFmpeg 优先截取 `BOBO_COVER_CAPTURE_SECS` 指定秒数，失败后回退到首帧；输出固定为居中裁切的 `640×360` 有损 WebP。
- 自动封面缓存文件名由稳定视频 ID、视频长度和修改时间共同决定；同一视频重复扫描直接复用，视频替换后生成新版本，失效缓存由服务清理。
- FFmpeg 无法启动、视频无法解码或未产生有效图片时使用应用默认封面并输出包含相对路径的中文警告，视频仍进入列表。
- 视频不可读或格式不受支持时不进入列表；服务继续运行并输出包含相对路径的中文警告。
- 忽略以 `.` 开头的隐藏文件和目录、临时文件以及符号链接，不跟随媒体根目录之外的路径。

### 4.2 视频标识与顺序

- 视频 ID 由“媒体根目录内的规范化相对视频路径”计算 SHA-256，并取前 16 字节的十六进制字符串，共 32 个小写字符。
- 相对路径分隔符统一规范为 `/`；文件相对路径不变时，服务重启或目录重新扫描后 ID 保持不变。
- 视频重命名或移动到其他文件夹后视为新视频，ID 随之变化。
- 首页和播放顺序按“相对文件夹路径 + 视频文件名”进行不区分大小写的数字自然升序排序，使 `2-视频.mp4` 排在 `10-视频.mp4` 之前。
- 首页先按分类路径自然升序排列分区，再在每个分类内按视频文件名自然升序展示；嵌套文件夹以完整相对路径作为分类名，避免同名末级目录混淆。
- 不通过额外清单或数据库覆盖标题、ID、封面和排序；需要调整标题或顺序时直接重命名文件。

### 4.3 扫描与运行时刷新

- 服务绑定端口前执行一次完整递归扫描；媒体根目录不存在、不可读或无法规范化时启动失败并输出中文原因。
- 空媒体目录允许服务正常启动，客户端显示空状态。
- 服务启动后递归监听媒体根目录的新增、写入、重命名和删除事件。
- 文件变化停止并达到 `BOBO_SCAN_DEBOUNCE_MS` 后执行完整重扫，避免大文件复制过程中频繁刷新内容列表。
- 服务同时按 `BOBO_SCAN_INTERVAL_SECS` 执行周期完整扫描，保证 Docker Desktop、网络盘或其他无法可靠传递文件事件的 bind mount 仍能发现变更。
- 事件扫描与周期扫描必须合并调度，同一时间最多执行一个完整扫描，避免重复遍历和快照覆盖竞态。
- 扫描成功后一次性原子替换内存中的视频目录快照，API 请求不得看到半更新状态，也不得在每次列表请求时重新遍历磁盘。
- 运行时扫描失败时保留上一份成功快照并输出中文错误；后续文件事件仍可再次触发扫描。
- 视频复制完成后无需重启服务即可进入列表；存在人工封面时使用人工封面，否则生成或复用自动封面。视频被删除后，该条目在下一次成功扫描后移除。
- 使用默认 Docker 配置时，宿主机资源变更应在文件写入停止后的 15 秒内反映到视频列表。
- 已经开始的视频响应允许依靠已打开的文件句柄完成；文件移除后发起的新请求返回 404。

### 4.4 媒体格式

- 视频统一采用 MP4 容器、H.264 视频编码、AAC 音频编码。
- 封面支持 WebP、JPEG、PNG。
- 除生成单帧 WebP 自动封面外，服务端不执行视频转码；编码不兼容时由客户端显示中文播放失败状态。
- 正式媒体由项目方后续以文件夹方式放入媒体根目录，本仓库初始版本不附带演示视频或正式封面。

## 5. 后端接口契约

接口前缀统一为 `/api/v1`，字段采用 camelCase。

### 5.1 健康检查

`GET /healthz`

```json
{
  "status": "ok"
}
```

### 5.2 视频列表

`GET /api/v1/videos`

```json
{
  "items": [
    {
      "id": "colors",
      "title": "认识颜色",
      "folderPath": "颜色启蒙",
      "coverUrl": "/api/v1/videos/colors/cover",
      "streamUrl": "/api/v1/videos/colors/stream"
    }
  ]
}
```

- 返回顺序必须与媒体自动发现后的自然排序一致。
- `folderPath` 为媒体根目录内的相对父文件夹路径；根目录视频返回空字符串。客户端必须兼容旧服务缺少该字段的情况并按“未分类”处理。
- 首版不分页。

### 5.3 视频封面

`GET|HEAD /api/v1/videos/{id}/cover`

- 通过视频 ID 定位当前自动发现快照中的封面，不接受任意文件路径。
- 返回人工封面、缓存的自动 WebP 或应用默认封面之一，客户端不需要区分来源。
- 正确返回 MIME、文件长度和缓存相关响应头。

### 5.4 视频流

`GET|HEAD /api/v1/videos/{id}/stream`

- 无 Range 时返回完整媒体和 `200 OK`。
- 有合法 Range 时返回 `206 Partial Content`。
- 返回 `Accept-Ranges: bytes`、正确的 `Content-Length` 和 `Content-Range`。
- 无法满足的 Range 返回 `416 Range Not Satisfiable`。
- 浏览器和 Flutter 播放器必须能够通过该接口拖动和跳转播放位置。

### 5.5 错误响应

```json
{
  "error": {
    "code": "video_not_found",
    "message": "没有找到这个视频"
  }
}
```

- API 未匹配路径返回 JSON 404，不允许被 Flutter Web 的 SPA 回退接管。
- 服务日志、可展示错误消息和启动失败原因使用中文。

### 5.6 Web 托管、反向代理与跨域

- Client 容器中的 Nginx 托管 Flutter Web Release，Rust Service 不携带或托管 Web 静态文件。
- Nginx 对非 API 前端路径回退到 `index.html`，支持 Web 页面刷新；`index.html` 与 Service Worker 禁止长期缓存。
- Nginx 将 `/api`、`/healthz` 反向代理到 Service，并关闭视频代理缓冲，完整保留 `Range` 与 `If-Range` 请求。
- 正式 Web 使用同源相对 `/api/v1`。
- 公开只读接口允许跨域 `GET`、`HEAD` 和 `Range`，便于 Flutter Web 开发环境联调；不允许凭据型跨域请求。

### 5.7 Android 升级清单与安装包

`GET /api/v1/app-updates/latest`

- `Service/updates/latest.json` 不存在时返回 `204 No Content`，表示当前没有可发布更新。
- 清单存在且合法时返回 `versionName`、递增的 `versionCode`、`apkFile` 对应的 `downloadUrl`、`sha256`、`sizeBytes`、中文 `releaseNotes` 和 `publishedAt`。
- 清单禁止携带任意外部下载地址；服务端只根据安全文件名生成同源下载地址，防止客户端被引导到非受控来源。
- 清单使用 `Cache-Control: no-store`，发布脚本必须先完成版本化 APK，再以原子替换方式更新 `latest.json`。

`GET|HEAD /api/v1/app-updates/packages/{fileName}`

- 只允许下载当前有效清单精确引用的 `.apk` 文件，拒绝目录分隔符、路径穿越和其他文件类型。
- 支持 `Range`、`HEAD`、正确的 APK MIME、文件长度及版本化长期缓存。
- APK 的真实长度必须与清单 `sizeBytes` 一致；客户端还必须在安装前校验 SHA-256、包名、版本号和 Android 系统签名。

## 6. Flutter 功能与交互

### 6.1 API 地址

- APK 与 Web 未显式配置时统一使用 `https://wx.jiayuntong.com:5172/server/` 服务根地址，并自动补齐 `/api/v1/`。
- 本地开发可使用 `--dart-define=API_BASE_URL=...` 覆盖 Rust 服务地址；参数既可传服务根地址，也可传完整 `/api/v1/` 地址。
- Android Debug 允许访问局域网 HTTP；Release 默认要求 HTTPS。

### 6.2 Android 静默升级

- 升级能力只在 Android 启用，Flutter Web 不检查、不下载，也不显示更新入口或弹窗。
- 应用启动及从后台恢复时检查最新清单；同一前台会话避免并发或高频重复请求。
- 仅当远端 `versionCode` 严格大于当前安装版本时自动进入后台下载；相同或更低版本不得覆盖安装。
- 已发布的 `versionCode` 与 APK 内容必须保持不可变；发布脚本检测到同版本 APK 哈希变化时必须拒绝覆盖，并提示先提升应用版本号。
- 下载由 Android 系统 `DownloadManager` 执行，Flutter 页面不显示阻塞式进度界面；系统可按平台要求展示下载通知。
- 下载任务 ID、目标版本和校验信息由 Android 端持久化；应用进程重启后能够恢复查询，不重复下载同一版本。
- 下载完成后校验 SHA-256、APK 包名和目标 `versionCode`；任何一项不匹配都删除无效包并显示中文失败提示，不得弹出安装提示。
- 校验通过后弹出符合现有设计语言的更新对话框，展示版本和中文更新说明，提供“稍后”和“重启并安装”。
- 点击“重启并安装”后使用 Android 系统安装器；Android 8 及以上如尚未授权此来源安装应用，先进入对应系统设置，用户授权返回后继续唤起安装器。
- 普通 Android 应用不能绕过系统完成无确认安装；除企业设备所有者或 Root 场景外，用户仍需在系统安装器确认。
- Release 清单和 APK 必须通过 HTTPS 获取；升级 APK 必须与已安装应用使用同一正式签名。当前调试签名只允许本地安装链路验收。

### 6.3 总首页与一级分类

- 应用启动后先进入“菠萝乐园”总首页，不直接请求或展示早教视频书架。
- 总首页固定展示菠萝早教、菠萝视频、菠萝相册三个一级分类，每个入口同时包含完整可见的统一风格矢量图标、中文标题、简短用途说明和明确进入提示，整个入口卡片均可点击；Web Release 不得因图标字体裁剪而只显示彩色底块。
- 点击菠萝早教进入原视频书架页面；该页面作为子页面，顶部只显示不小于 56dp 的返回按钮和“菠萝早教”标题，不显示品牌图标或标题下方的小文案，返回后恢复总首页及原滚动位置。
- 点击菠萝视频或菠萝相册进入各自的预留子页面；顶部只显示返回按钮及对应的“菠萝视频”或“菠萝相册”标题，不显示品牌图标或标题下方的小文案；在业务模型未确认前，页面主体继续展示童趣状态说明，不读取或冒充菠萝早教数据。
- 总首页在小于 680px 的窗口使用单列横向入口卡；中等宽度最多两列，大于等于约 980px 时使用三列等宽布局，并限制桌面最大内容宽度。
- 总首页和三个入口均提供无障碍语义、键盘焦点、鼠标指针与清晰按压反馈；所有触控目标不小于 48dp，页面不依赖颜色传递分类含义。

### 6.4 菠萝早教子页面

- 启动后请求视频列表，并按 `folderPath` 形成纵向分类分区；每个分区内使用单行横向滚动书架展示视频卡片。
- 分类标题展示完整相对文件夹路径和该分类的视频数量；根目录视频归入“未分类”，不同文件夹的视频不得揉成一个无分区网格。
- 分类分区和分区内视频沿用后端自然排序；空文件夹不显示，只有一个分类时仍显示分类标题，确保内容来源清晰。
- 首页每个分类最多展示 5 个位置：分类视频不超过 4 个时全部展示；超过 4 个时展示前 4 个视频，第 5 个位置固定显示“查看更多”，并明确剩余数量；该规则不随屏幕宽度增加而扩大。
- 横向书架支持触摸、鼠标拖动和触控板滚动；不同分类保持独立滚动位置，不允许横向书架截断卡片触控区域。
- 点击“查看更多”进入该文件夹的完整列表子页面；子页面使用响应式平铺网格展示全部视频，顶部必须提供清晰的返回键、完整文件夹名称和视频总数。
- 从完整列表页点击视频进入播放器时，播放器仍使用该文件夹的完整视频列表；返回键先回到完整列表页，再返回首页。
- 首页支持下拉刷新；从播放页返回首页或应用从后台恢复时静默重新请求视频列表，使后端新发现或已移除的内容能够被客户端更新。
- 静默刷新失败时保留当前列表并给出非阻塞中文提示，不把已有内容替换为全屏错误态。
- 完整列表页手机默认两列，平板和桌面根据可用宽度增加列数；首页书架卡片根据可用宽度采用稳定尺寸，不随大屏无限拉伸。
- 可用宽度小于 600px 时，首页顶部使用“品牌区 + 紧凑视频统计条 + 独立刷新按钮”的纵向结构；统计条和刷新按钮高度均不小于 56dp，不允许宽大的桌面统计卡片单独挤占一行。
- 可用宽度小于 600px 时，分类头第一行只展示文件夹图标和两行分类名称，第二行缩进展示数量标签与分隔线；数量标签不得与长分类名称争抢首行宽度。加载骨架必须采用相同分层结构。
- 每个卡片展示封面和清晰的视频标题，点击整个卡片进入播放页；标题最多显示两行，并始终预留完整两行高度，单行标题下方保留第二行空间，确保同一书架和完整列表中的卡片封面、标题区及底边严格对齐。
- 触控目标不小于 56dp，避免密集小图标和复杂导航。
- 首次请求期间必须显示与“分类标题 + 横向卡片书架”最终结构一致的骨架屏，减轻慢网络下的页面跳变；必须同时覆盖空清单、接口失败、重试和封面失败占位状态。

### 6.5 播放页面

- 使用独立沉浸式页面，不在首页列表内原位展开，也不触发浏览器或系统原生全屏。
- 页面以纵向 `PageView` 承载当前分类的视频，按该文件夹内的自然排序上下滑动，不切换到其他文件夹。
- 视频按原比例居中显示并使用 `contain`，不裁切内容。
- 首次由点击首页封面触发播放；切换到相邻视频后自动播放。
- 首尾不循环；继续向外滑时停留当前位置，并显示“已经是第一个啦”或“已经是最后一个啦”等短提示。
- 播放结束后停留在当前视频并显示大号重播按钮，不自动切换下一条。

### 6.6 播放控制

- 点击视频画面切换播放/暂停，并显示短暂的中央播放或暂停反馈。
- 底部展示当前时间、总时长和可交互进度条。
- 支持点击进度条定位，也支持拖动调整播放位置。
- 拖动开始时记录当前播放状态并暂停；拖动结束后完成跳转，拖动前正在播放则继续播放。
- 进度条、返回按钮等控件必须阻止事件穿透，不能误触发画面播放切换。
- 播放中控件延迟淡出；暂停、缓冲、错误时保持必要控件可见。
- 覆盖初始化、缓冲、播放失败和重试状态。

### 6.7 播放器资源管理

- 仅保留当前视频和相邻视频的播放器实例，避免大量控制器长期占用内存。
- 页面切换时立即暂停旧视频，并播放已预加载的新视频。
- 离开播放页时释放全部播放器资源。
- 快速连续滑动时使用请求序列或等效机制丢弃过期异步结果，避免串页播放和已释放控制器回调。
- Web 浏览器拒绝带声音自动播放时，页面展示醒目的中央播放按钮，不陷入无限重试。

## 7. UI 与设计规范

### 7.1 视觉方向

- 整体方向：简单、童趣、柔和、像玩具积木，但不堆砌装饰。
- 首页使用奶油白背景，青绿色为主色，珊瑚橙、阳光黄和天空蓝作为点缀。
- 使用统一的大圆角、轻柔阴影、平衡留白和少量几何装饰。
- 不使用紫色渐变、密集卡片、细小文字、突兀图标或过度动画。
- 默认亮色模式，不以暗色模式作为首版验收范围。

### 7.2 总首页与菠萝早教页规范

- 总首页顶部显示“菠萝乐园”、卡通菠萝品牌图标和简短中文欢迎文案；入口区域只保留儿童可直接理解的“今天想去哪里玩？”，使用正文层级字体，不再显示附加说明文案。
- 三个一级分类采用同一圆角、阴影、间距和图标结构，通过故事绿、活力橙、天空蓝形成易辨识但不过度饱和的独立主题；结构图标统一使用 Material 圆角矢量图标，不使用 Emoji。
- 手机端入口卡采用插图在左、标题与说明在右的紧凑横向结构；桌面端使用三列纵向卡，三张卡严格等宽、等高和对齐。
- 三个一级分类子页面使用统一标题栏：返回按钮后直接显示对应分类标题，不显示分类图标和标题下方的小文案；预留页主体状态插图与状态说明不受标题栏精简影响。
- 每个文件夹分类使用文件夹图标、清晰标题和轻量数量标签形成分区标题；分类之间保留 28–36dp 间距，不使用厚重分组外框。
- 横向书架保持单行、统一卡片宽高与 14–18dp 间距；“查看更多”使用与视频卡片一致的尺寸和圆角，但通过文件夹图标、剩余数量和箭头明确区分。
- 小屏首页减少无效纵向留白，统计、刷新、分类名称和分类数量形成清晰的两级信息结构；长分类名优先获得整行可用宽度，不被分隔线压缩成狭窄文本列。
- 完整列表页延续奶油白、青绿色和大圆角设计语言；顶部返回键触控区域不小于 56dp，页面标题过长时允许两行或省略显示。
- 封面保持一致比例，卡片圆角、标题间距和阴影统一；标题区域固定占两行高度，短标题不得挤占第二行空间或改变封面高度。
- 卡片按压反馈清晰但不过度弹跳。
- 平板和桌面保留合理最大内容宽度，避免卡片无限拉伸。

### 7.3 播放页规范

- 使用深海军蓝背景突出视频内容。
- 返回按钮、标题、进度条、时间和播放反馈形成统一控制层。
- 控件适配安全区域，避免被 Android 系统栏或 iPhone Safari 浏览器栏遮挡。
- 动效优先使用淡入淡出和轻量缩放，避免影响播放性能。

### 7.4 可用性与无障碍

- 文本和图标保持足够对比度。
- 按钮、卡片和播放器控制提供语义标签。
- 状态提示使用儿童可理解的中文表达，同时避免依赖颜色传递唯一含义。

## 8. 测试与验收

### 8.1 Rust 自动化测试

- 空目录、单目录多个视频、嵌套目录、根目录视频、分类路径提取、同名封面配对、人工封面优先级、缺失封面自动生成、缓存复用、视频变化缓存失效、生成失败默认封面、隐藏文件、符号链接和非法路径。
- 相对路径哈希 ID 的稳定性、不同目录同名视频的 ID 唯一性、数字自然排序、未知 ID、封面 MIME 和 `HEAD` 请求。
- 目录新增、复制完成、重命名、删除、防抖合并、扫描失败保留旧快照和成功后的原子替换。
- 完整视频下载、起止 Range、开放结尾 Range、后缀 Range、非法 Range 和 `416`。
- 健康检查、API JSON 404、Service 非 API 路径 JSON 404 和 CORS 响应头。
- 升级清单不存在时的 `204`、合法清单响应、非法清单拒绝、APK 精确白名单、`HEAD` 与 `Range` 下载。
- 执行 `cargo fmt --check`、`cargo clippy -- -D warnings`、`cargo test`。

### 8.2 Docker 自动化与部署验收

- 执行 `docker compose config`，确认环境变量默认值、端口、只读媒体与升级包 bind mount、可写自动封面命名卷、健康检查和安全选项有效。
- 分别执行 Client 与 Service 多阶段构建，确认两个最终镜像职责独立、不包含编译工具、本地媒体、自动封面缓存或无关仓库内容，且 Service 镜像包含可用 FFmpeg。
- 使用空的宿主机 `Service/media` 启动 Compose，两个容器必须健康，Web 首页必须显示空状态。
- 容器运行时向宿主机映射目录加入“仅视频”和“视频 + 人工封面”两类资源，API 应在 15 秒内更新且无需重启容器；前者返回自动封面，后者返回人工封面。
- 在文件事件不可用的测试场景中，周期扫描仍必须发现新增、重命名和删除。
- 容器内媒体挂载必须为只读；以容器进程尝试写入资源目录必须失败，宿主机资源不得被服务修改。
- 自动封面缓存卷必须可由非 root 服务用户写入并在容器重建后复用；根文件系统其他位置仍不可写。
- 删除并重建容器后，宿主机资源保持不变并可被重新发现；镜像升级不得影响媒体目录。
- 通过前端 `5170` 验证 Flutter Web、同源列表、封面和 `206/416` Range 代理；通过后端 `5171` 直接验证健康检查与 API。
- 检查两个容器均以非 root 用户运行、根文件系统只读、健康检查正常、`unless-stopped` 重启策略和停止宽限期生效。
- 在 Windows Docker Desktop 实际执行一次 bind mount 新增与删除资源验收。

### 8.3 Flutter 自动化测试

- API JSON 的 `folderPath` 解析、旧服务字段缺失兼容、异常映射和 Repository URL 解析。
- 总首页三个一级分类、390px 单列布局、1912px 三列等宽布局、菠萝早教进入与返回链路、预留子页面状态和返回层级 Widget 测试。
- 菠萝早教 ViewModel 的加载、文件夹分组、根目录“未分类”、成功、空态、失败、重试、下拉刷新和静默刷新失败保留旧数据状态。
- 菠萝早教分类标题、分类数量、600px 以下紧凑统计栏与分层分类头、横向书架、鼠标与触摸滚动、5 个位置上限、超过 4 个时“前 4 个 + 查看更多”、不超过 4 个时无入口、分区内播放器列表、固定两行标题槽位、结构一致的骨架屏、空态、错误态、重试、下拉刷新、返回页面刷新和点击封面 Widget 测试。
- 完整文件夹列表页的全部视频网格、响应式列数、顶部返回键、点击视频进入完整分类播放器列表和返回层级 Widget 测试。
- 使用假播放器验证点击暂停、进度跳转、上下滑切换、首尾边界、播放结束和资源释放。
- 升级清单解析、版本比较、无更新、静默入队、下载状态恢复、校验失败、准备完成提示、“稍后”及“重启并安装”交互测试。
- 执行 `flutter analyze`、`flutter test`、Android Debug 构建和 Flutter Web Release 构建。

### 8.4 手工验收

- Android 手机或模拟器。
- `390 × 844` 手机 Web 画布。
- `1912 × 914` Flutter Web 大画布，检查响应式布局上限和视觉完整性。
- 总首页分别验证三个入口的点击反馈、桌面同排对齐、手机单列无横向溢出、菠萝早教返回总首页以及菠萝视频/相册预留页返回路径。
- 使用不提交仓库的临时本地样片验证真实解码、声音、进度拖动和连续切换。
- 最终在实际 iPhone Safari 验证播放、进度跳转、安全区域和自动播放兜底。
- Android 真机验证应用退到后台后继续下载、进程恢复不重复下载、未知来源授权往返、同签名 APK 覆盖安装和升级后版本号递增。

## 9. 已确认假设与约束

- 初始正式媒体目录为空，首次启动显示精致空状态。
- 仓库不包含演示视频或正式封面；真实播放验收依赖后续提供的正式素材或临时本地样片。
- 正式内容通过文件夹自动发现，不再维护 `catalog.json` 或其他视频清单。
- 一个文件夹可以包含多个视频；同目录、同基础文件名的人工封面可选，缺失时由服务自动截帧。
- 媒体文件夹是菠萝早教子页面的二级分类来源，不维护额外分类清单；嵌套目录显示完整相对路径，根目录视频显示为“未分类”，播放器上下滑仅限当前分类。
- 菠萝早教分类书架最多占用 5 个展示位置；视频超过 4 个时第 5 个位置用于“查看更多”，因此直接展示前 4 个视频，完整列表页始终展示该分类全部视频；大屏不得增加预览数量。
- 菠萝早教与完整文件夹页复用同一视频卡片；标题最多两行且标题区域固定预留两行高度，短标题不缩短卡片底部区域。
- 菠萝视频和菠萝相册当前只有预留入口与状态页；在数据来源、媒体类型和接口契约确认前，不读取菠萝早教视频列表。
- 服务没有认证和写接口，内容按局域网或公开只读资源设计。
- 正式部署以 Docker Compose 为唯一基线；Client Web 与 Rust Service 使用两个职责独立的镜像和容器，均不包含媒体资源。
- 宿主机媒体目录以只读 bind mount 映射到容器 `/app/media`，资源可独立于容器创建、更新和备份。
- 自动封面保存在独立命名卷 `/app/cover-cache`，不写回媒体目录；缓存可删除并自动重建，不属于正式媒体备份范围。
- 首版播放器是应用内沉浸式页面，不使用系统原生全屏，以保留进度控件和上下滑切换。
- 首尾不循环，播放完成不自动切换下一条。
- 两个 Docker 容器自身只提供 HTTP；Client 内部 Nginx 仅负责同源 API 代理，生产 HTTPS 仍由部署栈外部网关终止。CDN、对象存储、转码和管理后台属于后续版本范围。
- Android 升级包独立于容器镜像发布到宿主机 `Service/updates`，Service 只读访问；历史 APK 不自动删除，只有 `latest.json` 当前引用的版本能通过接口下载。
- 普通 Android 自托管升级遵守系统安装确认和未知来源授权，不承诺企业设备所有者权限之外的完全静默安装。

## 10. 交付与文档要求

- 所有代码文件使用 UTF-8。
- 所有代码注释、终端提示、界面状态和交付说明使用中文。
- 根目录 README 必须说明 Docker/Compose 前置条件、镜像构建、启动停止、端口配置、媒体与升级目录映射、Android 升级包发布脚本、签名约束、人工封面优先与自动截帧规则、缓存卷、自动发现时限、日志与健康检查、Android API 地址和验收命令。
- 实现不得修改或删除现有 `提示词.md`，该文件作为原始需求输入留存。
- 后续接口、范围或默认行为如与本文冲突，必须先升级需求版本并留下变更记录。

## 11. 技术依据

- [Flutter 官方 video_player 包说明](https://pub.dev/packages/video_player)
- [Flutter Web 播放器限制与自动播放、Range 说明](https://pub.dev/documentation/video_player_web/latest/)
- [tower-http ServeDir 文档](https://docs.rs/tower-http/latest/tower_http/services/fs/struct.ServeDir.html)
- [tower-http Range 请求处理源码](https://docs.rs/tower-http/latest/src/tower_http/services/fs/serve_dir/mod.rs.html)
- [walkdir 递归目录遍历文档](https://docs.rs/walkdir/latest/walkdir/)
- [notify 文件系统事件监听文档](https://docs.rs/notify/latest/notify/)
- [sha2 哈希实现文档](https://docs.rs/sha2/latest/sha2/)
- [Docker bind mount 官方文档](https://docs.docker.com/engine/storage/bind-mounts/)
- [Docker Compose 服务、挂载、健康检查与重启策略](https://docs.docker.com/reference/compose-file/services/)
- [Docker 多阶段构建官方文档](https://docs.docker.com/build/building/multi-stage/)
- [Docker 镜像构建最佳实践](https://docs.docker.com/build/building/best-practices/)
- [FFmpeg 单帧提取、`-ss` 与 `-frames:v` 官方说明](https://ffmpeg.org/ffmpeg.html)
- [FFmpeg image2 单图片输出官方说明](https://ffmpeg.org/ffmpeg-formats.html#image2-1)
- [FFmpeg WebP 编码支持说明](https://ffmpeg.org/general.html#Image-Formats)

## 12. 版本记录

| 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|
| V0.1 | 2026-09-01 | 初始需求 | 固化产品范围、技术架构、接口契约、播放器交互、UI 规范与验收标准 |
| V0.2 | 2026-09-01 | 初始需求 | 取消人工维护视频清单，增加多视频文件夹、同名封面配对、递归扫描和运行时自动发现要求 |
| V0.3 | 2026-09-01 | 初始需求 | 将 Docker Compose 确认为正式部署方式，增加宿主机媒体目录只读映射、周期扫描兜底、安全运行和容器化验收要求 |
| V0.4 | 2026-09-01 | 初始需求 | 人工封面调整为可选；增加 FFmpeg 自动截取、版本化 WebP 缓存、生成失败默认封面和独立缓存命名卷 |
| V0.5 | 2026-09-01 | 初始需求 | 增加按媒体文件夹路径分区展示、根目录“未分类”、`folderPath` API 字段和分类内上下滑播放规则 |
| V0.6 | 2026-09-01 | 初始需求 | 首页分类改为横向滚动书架；增加结构一致的加载骨架、最多 6 个位置、“查看更多”入口和带返回键的完整文件夹网格页 |
| V0.7 | 2026-09-01 | 初始需求 | 视频卡片标题区域固定占两行高度；单行标题保留第二行空位，使书架与完整列表中的卡片结构对齐 |
| V0.8 | 2026-09-01 | 初始需求 | 首页分类最多展示前 4 个视频；视频超过 4 个时第 5 位固定为“查看更多”，且不因屏幕变大增加预览数量 |
| V0.9 | 2026-09-01 | 初始需求 | 优化 600px 以下首页：顶部采用紧凑统计条与独立刷新按钮，分类名称和数量改为上下分层，骨架屏同步响应式结构 |
| V1.0 | 2026-09-01 | 品牌升级 | 产品与应用显示名统一为“菠萝早教”；浏览器 favicon、PWA/Android 应用图标及首页品牌图标统一使用卡通菠萝形象 |
| V1.1 | 2026-09-01 | 部署架构升级 | Flutter Web Client 与 Rust Service 拆分为两个独立容器；Client Nginx 代理同源 API，前端与后端默认发布到 `5170`、`5171` |
| V1.2 | 2026-09-02 | 信息架构升级 | 新增“菠萝乐园”总首页，固定展示菠萝早教、菠萝视频和菠萝相册三个一级分类；原视频书架改为可返回的菠萝早教子页面，另外两个分类提供独立预留状态页 |
| V1.3 | 2026-09-02 | 品牌名称升级 | 产品名称、Flutter 应用标题、Android 安装名称、浏览器页签及 Web/PWA 显示名统一调整为“菠萝乐园”；“菠萝早教”继续作为一级业务分类名称 |
| V1.4 | 2026-09-03 | Android 升级管线 | 增加同源升级清单与版本化 APK 接口、Android 系统后台下载、完整性与包信息校验、下载完成提示、未知来源授权和系统安装器流程 |
| V1.5 | 2026-09-03 | 正式接口入口 | APK 与 Flutter Web 的默认服务根地址统一为 `https://wx.jiayuntong.com:5172/server/`，构建时自动补齐版本化 API 路径并允许本地显式覆盖 |
| V1.6 | 2026-09-03 | 门户视觉精简 | 修复 Web Release 一级分类图标字体裁剪；总首页入口提示改为单行正文；三个一级分类子页面统一为“返回按钮 + 分类标题”，移除标题栏图标和附加小文案 |
